:80 {
  root * /root/ui
  file_server

  encode gzip

  log {
    output stdout
    format console
  }

  # 代理华相的 API：保持原有 /v1 前缀转发到 127.0.0.1:8080
  handle /v1* {
    reverse_proxy 127.0.0.1:8080
  }
}
